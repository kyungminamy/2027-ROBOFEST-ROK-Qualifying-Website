/**
 * 페이지 좌우 여백과 최대 너비.
 *
 * 모든 구역이 같은 폭을 쓰도록 한 곳에서 관리합니다.
 * 여백을 넓히거나 좁히려면 이 값만 바꾸세요.
 *
 * ⚠️ 이것은 '가운데 정렬된 좁은 칸'입니다. 본문(소개·종목·일정 등)이 씁니다.
 *    상단 메뉴와 첫 화면은 아래 edgePadding 을 씁니다.
 */
export const container = "mx-auto w-full max-w-3xl px-5 sm:px-6";

/**
 * 화면 끝에서 글이 시작되기까지의 좌우 여백.
 *
 * ★ 상단 메뉴(SiteNav)와 첫 화면(Hero)이 '둘 다' 이 값을 씁니다. ★
 *   그래야 로고 왼쪽 끝과 대회 제목 왼쪽 끝이 정확히 한 줄로 맞습니다.
 *   ⚠️ 한쪽만 바꾸면 둘이 어긋납니다. 반드시 여기만 고치세요.
 *
 * 【 값을 이렇게 정한 이유 】
 *  · 휴대폰(px-5=20px)·태블릿(px-8=32px)은 고정값입니다.
 *    좁은 화면에서 비율로 주면 글 쓸 자리가 너무 줄어듭니다.
 *  · 1024px 이상은 화면 너비의 8%입니다.
 *    로보월드 누리집(robotworld.or.kr)의 여백을 실제로 재어 보니
 *    화면 1910px에서 좌우 155px, 즉 8.1%였습니다. 그 비율을 그대로
 *    따랐기 때문에 화면이 커질수록 여백도 함께 넓어집니다.
 *    (1024px → 약 82px, 1920px → 약 154px)
 */
export const edgePadding = "px-5 sm:px-8 lg:px-[8%]";

/**
 * 첫 화면(홈)의 각 구역이 쓰는 폭.
 *
 * 본문 페이지(container)보다 넓습니다. 홈은 '무엇이 있는지 훑어보는 곳'이라
 * 종목 카드를 한 줄에 3장까지 놓을 수 있어야 하기 때문입니다.
 *
 * ⚠️ 글(설명 문장)까지 이 폭으로 늘리지 마세요. 한 줄이 너무 길어져
 *    읽기 어려워집니다. 문장에는 아래 proseWidth 를 함께 쓰세요.
 */
export const homeSection = "mx-auto w-full max-w-5xl px-5 sm:px-8 lg:px-12";

/**
 * 문장(설명글)의 최대 폭.
 *
 * 한글은 한 글자가 영문보다 넓어서, 줄이 길면 눈이 다음 줄을 찾기 어렵습니다.
 * ⚠️ ch 단위(max-w-[46ch] 등)는 쓰지 마세요. ch 는 영문 '0' 한 글자를
 *    기준으로 재기 때문에, 한글에서는 의도한 것의 절반쯤에서 줄이 바뀝니다.
 */
export const proseWidth = "max-w-3xl";
